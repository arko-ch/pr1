import React from 'react';

const EmailAvatar = ({ children }) => {
  return (
    <div className="email-email-avatars">
      <div className="avatar-wrapper avatar-wrapper-overlap">{children}</div>
    </div>
  );
};

export default EmailAvatar;
