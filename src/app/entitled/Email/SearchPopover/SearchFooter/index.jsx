import React from 'react';
import { EmailButtonLink, SearchFooterContainer } from '../../StyledComponents';

const SearchFooter = () => {
  return (
    <SearchFooterContainer>
      <EmailButtonLink>Show all results</EmailButtonLink>
    </SearchFooterContainer>
  );
};

export default SearchFooter;
